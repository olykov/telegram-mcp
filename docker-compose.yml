services:
  telegram-mcp:
    image: "${APP_IMAGE}:${APP_TAG}"
    container_name: telegram-mcp
    env_file:
      - .env
    ports:
      - "8004:8000"
    networks:
      - core-infra
    restart: unless-stopped

networks:
  core-infra:
    external: true